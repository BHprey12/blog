const express = require("express")
const router = require("./routers/post.route.js")

const app = express()

app.use(route)


app.listen(4000, () => {
  console.log("server started !")
})
  model.post.create({
    username: "bruce"
  })